{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "JARVIS Cockpit core capabilities. Shell execution is restricted to the codex/git/launchctl allowlist (§9); arbitrary command execution is not permitted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "shell:allow-open"
  ]
}
